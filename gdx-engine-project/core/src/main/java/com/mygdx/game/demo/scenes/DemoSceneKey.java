package com.mygdx.game.demo.scenes;

/**
 * Demo-only scene keys.
 * IMPORTANT: keep these OUT of the engine package.
 */
public enum DemoSceneKey {
    MAIN_MENU,
    INSTRUCTIONS,
    GAME,
    PAUSE,
    GAME_OVER
}
